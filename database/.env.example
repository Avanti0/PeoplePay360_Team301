# Copy this file to .env and adjust to match your local PostgreSQL install.
# .env is git-ignored so each teammate/machine can have its own values.

PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=peoplepay360
